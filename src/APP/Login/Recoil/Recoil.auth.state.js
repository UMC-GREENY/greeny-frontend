import { atom } from 'recoil';

export const isSuccessState = atom({
  key: 'isSuccessState',
  default: false, 
});

// export const isAutoState = atom({
// 	key: 'isAutoState',
// 	default: false, 
// });